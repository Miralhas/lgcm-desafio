apiVersion: 1

providers:
  - name: 'provider name'
    folder: ""
    type: file
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true